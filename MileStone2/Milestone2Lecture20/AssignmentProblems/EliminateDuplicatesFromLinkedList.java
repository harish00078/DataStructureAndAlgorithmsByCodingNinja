package MileStone2.Milestone2Lecture20.AssignmentProblems;

import java.util.LinkedList;

public class EliminateDuplicatesFromLinkedList {

    // public static LinkedList<Integer> removeDuplicates(LinkedListNode<Integer> head){

    //     if(head == null){ // here we check if head is null:
    //         return head;// then we simply return head: 
    //     }

    //     LinkedList<Integer> currHead = head;  // here we create new Node currHead will equal to the head:

    //     while(currHead.next != null){ //here we are checking that if currHead's next NOde is null the then we simply return head also:
    //         if(currHead.data.equals(currHead.next.data)){ // here we are checking the duplicate value in the linkedlist:
    //             //if currHead value is equal to its next value:

    //             currHead.next = currHead.next.next;//then that next value will jump on the its next value:
    //             //for removing that next value:because it matches with the currHead value:

    //         }else{ // else if currHead value does not matches with it next value;
    //             currHead = currHead.next; // then we simply jump our currhead  to it next value:
    //         }
    //     }
    //     return head; //after all these we will simply return our head:
    // }
    
}
