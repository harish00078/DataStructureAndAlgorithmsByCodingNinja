package MileStone2.Milestone2Lecture20.AssignmentProblems;

import java.util.LinkedList;

public class FindANodeInLinkedList {

    // SOLUTION:-1

    // public static int findNode(LinkedList<Integer> head, int n) {
	// 	LinkedList<Integer> node = head; /// here we create new varible (node) and this variable will we equal to the head;

    //     int count=0; // here we create count function that will keep track on the linked list Nodes or we can say its index also:
    //     // because it will aslo counting the nodes:

    //     while (node!=null) // here we are looping in the linked list nodes util they  donot get null:
    //     {
    //         if (node.data==n) // here we are checking that if node data or value is equal to the  variable (n) value;
    //         {
    //             return count; // then we simply return the count value: becasue that is our index value also:
    //         }
    //         else //if node value is not equal to the (n) varaible value:
    //         {
    //             node=node.next; //then we simpply jump on the next value of the node:
    //             count++; // we aslo have to increment out count varaible:for keep track on the other node values:
    //         }
    //     }
    //     return -1;
    // }


    // SOLUTION:-2 => more simpler solution as compare to the first solution:
    
    // public static int findNode(LinkedList<Integer> head, int n) {

    //     int pos = 0;

    //     while(head != null){
    //         if(head.data.equals(n)){ // here we are using the data variable to check that it has equal value as(n) variable:
    //             return pos; //if it has a value equal to the (n):then we simply return the position of that value:
    //         }
    //         head = head.next; // if it is not equal to the (N) value then we simply jump on the next value of head:
    //         pos++; // we also have to update our pos variable: after checking the each Node in the linked list:
    //     }
    //     return -1;
    // }

    

	
    
}
