package MileStone2.Milestone2Lecture20.AssignmentProblems;

import java.util.LinkedList;

public class PrintReverseLinkedList {

    // public static void PrintReverse(LinkedListNode<Integer> root){

    //     if(root == null){ // here we firstly check that if root is null:
    //         return ; // then we simply do not need to return any thing:
    //     }

    //     // here  we call the recursion on the root.next values:
    //     // because first value will we handled by us:
    //     PrintReverse(root.next);  // here we reverse the root.next values of the linkedlist using recursion function:
    //     System.out.println(root.data + " "); // and here we are printing all the values of the linkedlist in reverse order:
    // }
    
}
