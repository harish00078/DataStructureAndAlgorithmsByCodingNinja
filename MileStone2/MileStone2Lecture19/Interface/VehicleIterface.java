package MileStone2.MileStone2Lecture19.Interface;

public interface VehicleIterface {
    /// heere we use interface class: for abstract methods.because interface class is a pure abstract class:
    // means in inteface class we were only use the abstract methods:


    // here we create some abstract methods in interface clas:
    // that we were use why user in other child classes:
    // when ever any user use this interface class.they has to gave answer to these two abstract methods:

    // abstract boolean isMotorized();

    // abstract String getcompany();


    // we can also create other functions in the interface classs:
    // int PI = 3.14;
    boolean isMotorized();

    String getCompany();

    
}
